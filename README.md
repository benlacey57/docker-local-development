# docker-local-development
Local development environment with Docker blueprints for creating client containers using the base docker blueprints. These will install the correct development tools depending on platform being used and there is a script to analyse PHP code, apply fixes and save the report.
